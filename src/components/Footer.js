import styled from 'styled-components'

const H = styled.h1`
    font-size: 5rem;
    font-weight: 500;
    font-family: monospace;
    text-align: center;
`

const Footer = () => {
    return (
        <div>
            this is a footer
            <H>Coming Soon...</H>
        </div>
    )
}

export default Footer
